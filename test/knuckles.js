import test          from 'blue-tape';
// import Knuckles from '../dist/knuckles';

test('Knuckles', t => {
  return Promise.resolve(true).then(t.ok);
  // t.test('should export Sonic', t => {
  //   return t.ok(Knuckles.Sonic);
  // });
  // t.test('should export Resource', t => {
  //   return t.ok(Knuckles.Resource);
  // });
  // t.test('should export XHR', t => {
  //   return t.ok(Knuckles.XHR);
  // });
});
